## zd incident list

List incidents (POST /api/incidents/filter)

### Synopsis

List incidents using Zenduty's filter endpoint. Defaults to OPEN incidents
(triggered + acknowledged) so the most common use case is one keystroke:

  zd incident list

Status flag values: open (default), all, triggered, acknowledged, resolved.
Pass --status multiple times for multi-status filters.

```
zd incident list [flags]
```

### Options

```
      --all-teams          include incidents across all teams (not just yours)
      --from string        from_date (YYYY-MM-DD)
  -h, --help               help for list
      --page int           pagination page number
      --priority strings   filter by priority unique_id (repeatable)
      --service strings    filter by service unique_id (repeatable)
      --status strings     status filter: open|all|triggered|acknowledged|resolved (repeatable) (default [open])
      --team strings       filter by team unique_id (repeatable)
      --to string          to_date (YYYY-MM-DD)
      --user strings       filter by assigned user username (repeatable)
```

### Options inherited from parent commands

```
      --account-id string   5-character account identifier; required for the 'zd event fire' command
      --config string       config file path (default: $XDG_CONFIG_HOME/zd/config.yaml or ~/.config/zd/config.yaml)
      --debug               log wire-level requests/responses to stderr
      --debug-curl          print a copy-paste curl reproduction of every request to stderr
      --host string         API base URL (default: https://www.zenduty.com)
      --no-color            disable color output
      --output string       output format: json | yaml | table | template (default: table on TTY, json when piped)
      --profile string      config profile name (default: profiles.default_profile in the config file)
      --template string     Go text/template body, used when --output=template
      --token string        API token (overrides env and profile)
```

### SEE ALSO

* [zd incident](zd_incident.md)	 - Triage, list, and update incidents

###### Auto generated by spf13/cobra on 6-May-2026
